services:
  - type: web
    name: maxparts-tienda
    env: node
    plan: free
    branch: master
    rootDir: server
    buildCommand: npm install && npm run build-css
    startCommand: node server.js
